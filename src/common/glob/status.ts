export enum StatusOrder {
    STARTED = 1,
    ASSIGNED = 100,
    TAKEN = 101,

    DELIVERED = 200,

    QUALIFIED = 300,

    CANCELLED = 400
}


export enum StatusPayment {
    STARTED = 1,
    CONFIRMED = 100,
    CANCELLED = 400
}